// The site's terminal frames are rendered by tcut in Catppuccin Mocha; the code blocks use the same palette.
// Code Hike's highlighter (lighter) takes a VS Code theme object, so the palette comes from shiki's theme set.
import catppuccinMocha from "@shikijs/themes/catppuccin-mocha";

export const codeTheme = catppuccinMocha;
