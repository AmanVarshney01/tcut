Symbols Nerd Font Mono (Nerd Fonts v3.5.1, symbols only, MIT — see LICENSE-nerd-fonts.txt).

The renderer loads it as the last font fallback so icons and powerline glyphs render even when no Nerd Font
is installed — the same symbols-only font Ghostty embeds (src/font/embedded.zig: symbols_nerd_font).
