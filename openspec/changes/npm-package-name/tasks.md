## 1. Rename

- [x] 1.1 `package.json` name → `termcut` (bin unchanged)
- [x] 1.2 CLI virtual module for both `tcut` and `termcut`
- [x] 1.3 README install lines + badge

## 2. Publish

- [ ] 2.1 `bun publish --access public` succeeds as `termcut@0.1.0` (interactive 2FA — run by the maintainer)
- [ ] 2.2 Verify `bunx termcut init demo` + `bun add -g termcut` from a clean directory
