## 1. Infra

- [x] 1.1 `packages/infra` Alchemy stack + root/turbo scripts; typecheck passes
- [x] 1.2 First production deploy; URL verified (fonts, SVG animation, assets)

## 2. Cleanup

- [x] 2.1 Remove Pages workflow and the GitHub Pages site; Astro `base` = `/`, `SITE` = Workers URL
- [x] 2.2 `deploy.yml` gated on Cloudflare secrets; README updated
